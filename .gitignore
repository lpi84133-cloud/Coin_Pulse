*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
/app/build/

# Signing — never commit these
/release.jks
/keystore.properties

# Seed and credentials — never commit these
/pulse.properties
/app/google-services.json

# Portfolio inventory snapshots — local audit artefacts
/_portfolio_*
/_apk_extract/
